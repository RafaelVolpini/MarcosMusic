# Copie para .env.local (dev) ou configure como variável de build no Railway (prod).
# Vazio/ausente em dev = usa caminho relativo + proxy do Vite (vite.config.ts) para localhost:8080.
# Em prod = URL pública do backend, ex.: https://marcosmusic-production.up.railway.app
VITE_API_URL=
